[project]
name = "gps-theorem-prover"
version = "0.1.0"
description = "A GPS-inspired theorem prover for propositional logic and real-analysis theorem schemas."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Axl Andrade" }]

[tool.black]
line-length = 88
target-version = ["py312"]
